{
  "name": "rerss-elm-ui",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "elm-app build",
    "debug": "elm-app start",
    "deploy": "npm run build && gh-pages -d build/ -o deploy",
    "install": "elm-app install",
    "start": "npm run debug"
  },
  "author": "Mathieu Agopian",
  "license": "APL",
  "devDependencies": {
    "elm": "^0.19.0-bugfix2",
    "elm-format": "^0.8.1",
    "gh-pages": "^1.1.0"
  }
}
